// Tailwind CSS v4 uses CSS-first configuration.
// Configuration is done in src/index.css using CSS variables.
// This file can be deleted or kept empty if not using the JS config compatibility mode.
export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};
